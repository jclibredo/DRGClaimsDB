  CREATE TABLE "DRG_SHADOWBILLING"."SEEKERUSERLOGS" 
   (	"ID" NUMBER NOT NULL ENABLE, 
	"USERNAME" VARCHAR2(50 BYTE) NOT NULL ENABLE, 
	"DATEACTION" TIMESTAMP (6) NOT NULL ENABLE, 
	"MODULE" VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	"ACTION" VARCHAR2(10 BYTE) NOT NULL ENABLE, 
	"DETAILS" VARCHAR2(255 BYTE) NOT NULL ENABLE, 
	 CONSTRAINT "SEEKERUSERLOGS_PK" PRIMARY KEY ("ID")
   );



CREATE SEQUENCE  "DRG_SHADOWBILLING"."SB_SEEKERUSERLOGS_SEQ"  MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE ;


create or replace TRIGGER "DRG_SHADOWBILLING"."SB_SEEKERUSERLOGS_TRIG" 
BEFORE INSERT ON SEEKERUSERLOGS
FOR EACH ROW
BEGIN
 SELECT SB_SEEKERUSERLOGS_SEQ.NEXTVAL
 INTO :NEW.ID FROM DUAL;
END;

